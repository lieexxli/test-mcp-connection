[project]
name = "test-mcp-connection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.0",
    "python-dotenv>=1.2.1",
]

[dependency-groups]
dev = [
    "googlemaps>=4.10.0",
]
